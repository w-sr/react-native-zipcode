import styled from 'styled-components/native';
import theme from '../../../theme';

export const StyledText = styled.Text`
  color: ${theme.colors.black.hex};
  font-weight: ${theme.fontWeights.regular.numerical};
`;
